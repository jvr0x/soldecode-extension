{
  "name": "soldecode-extension",
  "version": "1.0.0",
  "description": "Pre-signing Solana transaction decoder Chrome extension",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && cp manifest.json dist/ && cp -r assets dist/ && mkdir -p dist/popup && cp popup/popup.html dist/popup/",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@types/chrome": "^0.1.39",
    "typescript": "^6.0.2",
    "vite": "^8.0.6",
    "vitest": "^4.1.3"
  }
}
